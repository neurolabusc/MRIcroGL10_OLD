import gl
#gl.colorbartext(1)
#gl.colorbarsize(0.05)
#gl.colorbarposition(2)
#gl.clipazimuthelevation(0.5, 22, 45)
#gl.changenode(1,128,255,12,12,64)
#gl.cameradistance(0.5)
#print(gl.app_version())
#gl.resetdefaults()
#gl.savebmp('python.png')
#gl.backcolor(12, 12, 14)
print(gl.exists('mni152_2009_256'))
#gl.azimuth(122)
#gl.azimuthelevation(33,-20)
#gl.colorname('bone')
#gl.contrastminmax(12,40)
#gl.cutout(0,1,0,0.5,0,0.5)
gl.extract(3,2,1)