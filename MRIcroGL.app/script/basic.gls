BEGIN
	RESETDEFAULTS;
	return;
	LOADIMAGE('mni152_2009bet');
	OVERLAYLOAD('motor');
	OVERLAYMINMAX(1, 2.6, 5.0);
OVERLAYCOLORNAME(1, '1hot');

	//CLIPAZIMUTHELEVATION(0.3, 0, 130);
	//SHADERNAME('overlay');

END.

